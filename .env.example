# Origin público do crm-api (sem trailing slash, SEM /api/v1).
# Os paths /api/v1/... são adicionados pelos callers (Kubb e fetches server-side).
# Produção: https://api.digichat.digigov.com.br
NEXT_PUBLIC_API_URL=http://localhost:3000
